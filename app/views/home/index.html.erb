<div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
    <li data-target="#carousel-example-generic" data-slide-to="0" class="active"></li>
    <li data-target="#carousel-example-generic" data-slide-to="1"></li>
    <li data-target="#carousel-example-generic" data-slide-to="2"></li>
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox">
        <% @sliders.each.with_index do |slider, index| %>
            <!--DESTAQUE -->
            <% if index == 0 %>
                <div class="item active">
            <% else %>
                <div class="item">
            <% end %>
                <%= image_tag slider.image.image.slider.url %>

                <div class="container">
                    <div class="carousel-caption">
                        <% if slider.link.blank? %>
                            <h1> <%=slider.title%> </h1>
                        <% else %>
                            <h1> <%= link_to slider.title, slider.link %></h1>
                        <% end %>
                        <% if !slider.description.blank? %>
                            <p> <%=slider.description.first(180) + " (...)" %></p>
                        <%end%>
                    </div>
                </div>
            </div>
        <%end%>
  </div>

  <!-- Controls -->
  <a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">
    <span class="icon-prev"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">
    <span class="icon-next"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<div class="container">
    <div class="row text-center">
        
        <h2>Eventos MedHacker</h2>
        <hr>
    </div>


    <section>
        <div class="container">
        <div class="row">
            <ul class="grid cs-style-3">
        <% @events.each do |event| %>
            <li>
                <figure>
                    <%= image_tag event.image.image.project.url , class: "img-responsive" %>
                    <figcaption>
                        <h3><%= event.name%></h3>
                        
                            <span></span>

                            <%= link_to "Ver Evento", event_path(code: event.code)%>
                    </figcaption>
                </figure>
            </li>
        <%end%>
            </ul>
        </div>
        </div>
    </section><!--/#portfolio-item-->


    


</div>
<div class="container">
<div class="row">
  <div class="text-center"> <%= will_paginate @events, renderer: BootstrapPagination::Rails, previous_label: "Voltar", next_label: "Próxima" %> </div>
</div>
</div>

<div class="well">



<div class="container">
    <div class="row text-center">
        
        <h2>Conheça e acompanhe o MedHacker</h2>
        <hr>
         <%= image_tag "http://maps.google.com/maps/api/staticmap?size=2000x200&center=Brazil&sensor=true&zoom=3&scale=4#{Event.events_markers}",  height: "100%", width: "100%", class: "img-thumbnail"  %>
    </div>
</div>


</div>